	<ul class="list-group" id="omniauth-links">
	  <%- resource_class.omniauth_providers.each do |provider| %>
	  <li class="list-group-item"><%= link_to "Log in through #{$DOMAIN_CONFIG['site_name']}", root_url(:protocol => "http").gsub(/\/$/, "") + omniauth_authorize_path(resource_name, provider), :class => "btn btn-default" %></li>
	  <% end -%>

	  <li class="list-group-item"><a target="navsa" href="https://bavs.ac.uk/membership/">Not a BAVS member?</a></li>
	</ul>
